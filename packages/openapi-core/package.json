{
  "name": "@oncepass/openapi-core",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI contracts for Oncepass (common + identity + product domains)",
  "scripts": {
    "lint:domains": "redocly lint",
    "bundle:domains": "node bundle-domains.mjs",
    "lint": "pnpm run lint:domains",
    "bundle": "pnpm run bundle:domains"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.0"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module"
}

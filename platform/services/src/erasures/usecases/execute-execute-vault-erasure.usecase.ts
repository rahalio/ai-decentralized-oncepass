/**
 * ExecuteExecuteVaultErasure - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: erasures
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteExecuteVaultErasure Use Case
 *
 * DDD: Application use case for create Erasure.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ExecuteVaultErasureInput, ExecuteVaultErasureOutput } from "../dto/erasure.dto";
import type { ExecutionContextService, IdGeneratorService } from "@oncepass/services/_shared/index.js";
import type { ErasureRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteExecuteVaultErasure {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly erasure: ErasureRepository
  ) {}

  async execute(input: ExecuteVaultErasureInput): Promise<ExecuteVaultErasureOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ersId();

    // All field names extracted from OpenAPI spec: subjectRef, vaultId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!input.vaultId) {
      throw new ValidationError("vaultId is required");
    }    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (erasures domain)
    const entityId = this.idGenerator.ersId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.erasure.executeVaultErasure({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as ExecuteVaultErasureOutput;
  }
}
